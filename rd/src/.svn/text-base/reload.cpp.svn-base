/**
 * =====================================================================================
 *
 *	Filename:  reload.cpp
 *
 *	Description:  header file for reload.cpp
 *
 *	Version:  1.0
 *  Created: 2010-11-24
 *	Revision:  none
 *	Compiler:  gcc
 *
 *	Author:  zhangjunying, zhangjunying@baidu.com
 *	Company:  baidu.com Inc.
 *
 * =====================================================================================
 */


#include "reload.h"
#include "common.h"
#include "nshead.h"
#include "mc_pack_rp.h"
#include "idl/reload.idl.h"
#include "userworker.h"

using namespace idl;

/**
 * get request cmd from sock.
 * fd sock fd
 * pool memory pool
 */
reload_cmd* get_request(int fd, bsl::xcompool& pool)
{
	/**暴力kill 有问题找wensaiping*/
	return NULL;
}

/**
 * show the user list
 * cmd reload cmd struct pointer
 * sock_loader socket fd
 */
int show_list(reload_cmd *cmd, int sock_loader)
{
	/**暴力kill 有问题找wensaiping*/
	return 0;
}

/**
 * set the user list
 * cmd reload cmd struct pointer
 * sock_loader socket fd
 */
int set_list(reload_cmd *cmd, int sock_loader)
{
	/**暴力kill 有问题找wensaiping*/
	return 0;
}

/**
 * handle the reload cmd
 * cmd reload cmd struct pointer
 * sock_loader socket fd
 */
int handle_reload(reload_cmd *cmd, int sock_loader)
{
	/**暴力kill 有问题找wensaiping*/
	return 0;
}

/**
 * @function : reload site filter data
 * @param void
 * @return 0:ok; <0; error
 */
int reload_something()
{
  return -1;
}

/**
 *  @function reload thread function
 *  	difference singnal will lead to reloading difference data
 *
 *  @param[in]  无
 *  @param[out]  无
 *  @return 操作结果
 *  	-1    失败
 *  	0     成功
 */
void* svr_reload(void *arg)
{
	/**暴力kill 有问题找wensaiping*/
	return (void *)0;
}
